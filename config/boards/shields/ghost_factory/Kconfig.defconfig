if SHIELD_GHOST_FACTORY_LEFT

# Name must be less than 16 characters long!
config ZMK_KEYBOARD_NAME
    default "Ghost Factory"

config ZMK_SPLIT_ROLE_CENTRAL
    default y

endif

if SHIELD_GHOST_FACTORY_LEFT || SHIELD_GHOST_FACTORY_RIGHT

config ZMK_SPLIT
    default y

endif
